apiVersion: 1
datasources:
  - name: Prometheus
    # uid 를 고정한다. 대시보드 JSON 이 이 값을 참조하므로 자동 생성에 맡기면
    # 컨테이너를 다시 만들 때 패널이 전부 깨진다.
    uid: rtp-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
