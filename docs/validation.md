# 검증 결과

내 집계 캔들 vs 거래소 공식 캔들 대조.

> ## 이 수치가 무엇을 말하고 무엇을 말하지 않는가
>
> **말하는 것:** 아래 조건에서 집계 산식과 이벤트타임 처리가 공식 캔들과
> **정확히(근사 아님)** 일치했다. 소스 유실도 0이었다.
>
> **말하지 않는 것 — 표본이 좁다:**
> - 종목 2개(`005930`·`000660`). **국내 시총 1·3위, 틱이 끊이지 않는 종목**이다.
>   워터마크 실험의 결론("실시간 소비를 유지할 때 여유가 생긴다")이 가장 유리하게 작동하는 조건이다
> - 하루치, 정규장 초반 위주. **마감 동시호가 구간은 애초에 대조 대상이 아니다** —
>   거래가 없으면 이 파이프라인은 행을 만들지 않고 공식 캔들은 거래량 0 으로 행을 만든다.
>   정의가 다르다(`design-decisions.md` 참고). 그 밖에 거래 뜸한 구간도 대조하지 않았다 —
>   정작 PING 버그를 만난 구간이 후자다
> - 잡이 실시간을 유지한 상태에서만 쟀다. **밀렸다가 따라잡는 동안의 정확도는 모른다.**
>   그리고 아래 워터마크 실험이 밝힌 바로는 그 조건에서 결과가 달라진다
>
> 즉 **이 99% 는 "이 파이프라인은 정확하다" 가 아니라
> "이 조건에서 정확했고, 어긋난 4건의 원인까지 추적했다" 로 읽어야 한다.**

## 방법

- **대상**: `005930`(삼성전자), `000660`(SK하이닉스)
- **비교 항목**: O/H/L/C/V 전부. 가격은 `Decimal64(4)`, 거래량은 `Decimal64(8)` 로 **정확 비교**한다.
  부동소수 근사 비교를 쓰지 않는 이유는 그 순간 "허용 오차" 를 정해야 하고,
  그러면 진짜 오차가 허용 오차 뒤에 숨기 때문이다.
- **쿼리**: `rtp.candle_diff` 뷰 (`infra/clickhouse/init/02_views.sql`)
- **중복 판정**: `candles_1m_dedup.write_count`. 1이 아니면 같은 윈도가 두 번 쓰인 것

```sql
SELECT count() AS n_official,
       countIf(missing = 1)                      AS n_missing,
       countIf(missing = 0)                      AS n_matched,
       countIf(missing = 0 AND close_diff = 0)   AS n_close_exact,
       countIf(missing = 0 AND volume_diff = 0)  AS n_vol_exact,
       countIf(missing = 0 AND write_count > 1)  AS n_dup
FROM rtp.candle_diff;
```

## 결과

### 2026-08-31 · 첫 실측

| 항목 | 값 |
|---|---|
| 대조 구간 | 2026-08-31 08:00–08:43 (KST) |
| 집계한 체결 | 65,450건 |
| 대조된 윈도 | 88 |
| **시가 일치** | **88 / 88** |
| **고가 일치** | **88 / 88** |
| **저가 일치** | **88 / 88** |
| **종가 일치** | **88 / 88** |
| **거래량 일치** | **88 / 88** |
| 중복 기록 (`write_count > 1`) | **0** |

설정: 워터마크 지연 5초, 유휴 타임아웃 10초, 허용 지연 0초, 체크포인트 10초, 파티션 3.

**공식 캔들과 완전히 일치했다.** 근사가 아니라 Decimal 정확 비교 기준이다.

미매칭 306건은 전부 2026-08-28(금) 구간이다. 공식 캔들 API 가 200봉을 돌려주는데
수집기가 그 시점에 존재하지 않았다. 파이프라인 결함이 아니다.

### 2026-08-31 09:15 · 정규장 구간 재측정

| 항목 | 값 |
|---|---|
| 대조된 윈도 | 126 |
| 고가·저가 일치 | 126 / 126 |
| 종가 일치 | 125 / 126 |
| 시가 일치 | 123 / 126 |
| **거래량 일치** | **122 / 126** |
| 중복 기록 | 0 |

**어긋난 것은 09:05·09:06 두 분뿐이고, 그 시각은 내가 잡을 취소하고 재제출한 시점이다.**
앞뒤 윈도(09:03·09:04, 09:07~09:12)는 전부 정확히 일치한다.

차이는 **부호가 대칭이고 총량이 보존된다.**

| 윈도 | 종목 | 내 거래량 | 공식 | 차이 |
|---|---|---|---|---|
| 09:05 | 005930 | 148,625 | 151,013 | **−2,388** |
| 09:06 | 005930 | 387,392 | 385,004 | **+2,388** |
| 합계 | | 536,017 | 536,017 | **0** |

`000660` 도 −641 / +641 로 같은 모양이다. **유실이 아니라 경계 배정 차이다.**

#### 파이프라인 결백은 증명됐다

보관해 둔 원본 체결(`trades_raw`)에서 직접 집계한 값이 내 캔들과 **정확히 같다**.

```
09:05  raw_volume 148,625  = 내 캔들 148,625
09:06  raw_volume 387,392  = 내 캔들 387,392
```

즉 **Flink 집계는 자기 입력과 완벽히 일치한다.** 유실도 오배치도 없었다.
차이는 "내가 받은 체결의 타임스탬프" 와 "거래소가 봉을 나눈 기준" 사이에 있다.
`recv_seq` 연속성도 공백 0이므로 수집 구간 유실도 아니다.

#### 아직 설명하지 못한 것

경계 직후(`09:06:00.024`~`.062`) 체결 4건의 합이 **2,443** 인데 필요한 값은 **2,388** 이다.
55 만큼 어긋나므로 "시간 순 앞부분을 잘라 옮기면 맞는다" 는 설명이 성립하지 않는다.
경계 포함 규칙 차이(`(T-60s, T]` vs `[T, T+60)`) 가설도 기각됐다 —
127,631건 중 정각에 걸린 체결은 1건뿐이다.

확실한 단서는 하나다. 그 체결들의 **수신 시각이 이벤트 시각보다 이르다.**

```
event 09:06:00.024  <- 수신 09:05:59.943  (80ms 앞섬)
```

소스 시계가 우리보다 앞서 있다. 거래소 내부의 봉 분할 기준이 발행하는 틱 타임스탬프와
다를 가능성이 있으나 **아직 증거가 없다.** 다음 확인 대상으로 남긴다.

> 재제출하지 않은 구간에서는 이 현상이 없었다. 잡 재시작과의 인과도 아직 배제하지 못했다.

### 2026-08-31 · compose 전체 구간 (README 가 인용하는 수치)

위 두 표는 측정한 시점의 스냅샷이라 구간이 짧다. **README 와 `failure-policy.md`
가 인용하는 "566윈도 99.29%" 는 그날 누적 전체를 대상으로 한 것**인데,
그 근거 표가 이 문서에 없었다. 적대적 리뷰에서 "가장 앞세운 숫자가 검증 불가능하다" 는
지적을 받아 원본을 다시 떠서 싣는다.

`scripts/verify.sh` 의 3번과 **같은 쿼리**를 그날 볼륨(`rtp_clickhouse-data`)에
그대로 돌린 결과다.

```
matched:    566      ← 대조된 윈도 (08:00~12:52 KST, 2종목 = 283분 × 2)
open_ok:    563
high_ok:    566
low_ok:     566
close_ok:   565
vol_ok:     562      → 99.29%
duplicated: 420
```

- **거래량 99.29%** 가 README 의 수치다. 어긋난 4건은 잡을 취소하고 재제출한
  09:05·09:06 구간이고, 차이가 −2,388 / +2,388 로 대칭이라 유실이 아니라
  경계 배정 문제라는 것이 확인됐다(위 표 참고).
- **`duplicated: 420`** 은 결함이 아니라 관측 결과다. `--start-offsets earliest`
  로 재배포해 같은 윈도가 다시 쓰인 것이고, `MergeTree` 를 고른 덕에 보였다.
  `failure-notes.md` 의 "잡을 재시작했더니 같은 윈도가 두 번 쓰였다" 참고.

> 이 표를 뒤늦게 싣게 된 것 자체가 기록해 둘 만하다.
> **수치를 인용하면서 그 수치를 만든 쿼리를 같이 두지 않으면, 맞는 숫자도 못 믿을 숫자가 된다.**
> 이 프로젝트가 내세우는 것이 "결과가 맞는지 증명할 수 있는 구조" 인 이상 특히 그렇다.

### 2026-09-02 · K8s 파이프라인 첫 실데이터 검증

compose 에서 검증한 것을 K8s 로 옮긴 뒤 실제 장중 데이터로 다시 쟀다.

장 초반(09:00~09:36, 68윈도)에는 **OHLCV 전 항목이 68 / 68 로 완전 일치**했다.
아래는 관측 스택을 고치고 잡을 재배포한 뒤 10:10 까지 누적한 결과다.

| 항목 | 값 |
|---|---|
| 대조된 윈도 | 136 |
| 시가·고가·저가·종가 일치 | **전부 136 / 136** |
| 거래량 일치 | 134 / 136 (**98.53%**) |
| 중복 기록 | 0 |
| Kafka→Flink 구간 | 142윈도 중 138 일치 |
| 출처 오염 | 없음 (실수집 conn_id 1개) |

#### 어긋난 2건은 유실이 아니다

| 윈도 | 종목 | 내 거래량 | 공식 | 차이 |
|---|---|---|---|---|
| 09:37 | 005930 | 16,788 | 16,789 | **−1** |
| 09:38 | 005930 | 64,925 | 64,924 | **+1** |
| 합계 | | 81,713 | 81,713 | **0** |

**부호가 대칭이고 총량이 보존된다.** 종가도 두 윈도 모두 정확히 같다.
체결 한 건이 인접 윈도에 배정된 것이고, 앞서 09:05·09:06 에서 관측한 것과
완전히 같은 형태다. 이 패턴을 이미 한 번 겪어 두어서 이번에는 판정이 빨랐다 —
**유실이라면 한쪽 방향으로만 어긋나고 총량이 줄어든다.**

배포는 Helm 차트 + ArgoCD(`selfHeal`), 잡은 Flink Operator 의 `FlinkDeployment`,
체크포인트는 MinIO(S3), 워터마크 지연 5초·유휴 10초.

**표본이 136윈도다.** compose 검증(566윈도)만큼의 근거는 아니고,
"K8s 로 옮긴 뒤에도 같은 정확도가 나온다" 를 확인한 수준이다.
장 초반 70분 구간이라 마감 동시호가나 거래가 뜸한 구간은 여전히 대조하지 않았다.

이 구간에는 **잡 재배포가 한 번 포함돼 있다**(dead letter 를 고쳐 이미지를
0.1.1 로 올리고 savepoint 로 재개했다). 그런데도 중복이 0 이고 OHLC 가
전부 일치한다 — `upgradeMode: savepoint` 가 의도대로 동작한 것이다.
cancel 후 재시작했을 때 그 윈도가 −67% 였던 것과 대비된다.

#### 이 수치를 얻기 전에 한 번 망쳤다

첫 시도에서 거래량 일치율이 **56.6%** 였다. 파이프라인 문제가 아니라
**장애 실험에 쓴 합성 데이터가 운영 토픽에 섞여 있었다.**

```
실제 수집기   28,291건   가격 1,618,000 ~ 1,641,000
합성         278,644건   가격    66,563 ~    84,356
```

`close_diff` 가 −156만이었던 이유가 이것이다.
자세한 것은 `failure-notes.md` 의 "실험 데이터가 운영 토픽을 오염시켜 검증을 망쳤다".

> **`matched=106` 이었으므로 "정확도 주장 금지" 판정에도 걸리지 않았다.**
> 수치가 나왔고 그 수치가 틀렸다. `scripts/verify.sh` 에
> 출처 점검(1-b)을 추가한 이유다.

### 2026-09-02 · 하루치 K8s 실측 (장애 실험을 포함한 채로)

| 항목 | 값 |
|---|---|
| 대조된 윈도 | **760** (정규장 09:00~15:19 전체) |
| 시가·고가·저가·종가 | **전부 760 / 760** |
| 거래량 일치 | 754 / 760 (**99.21%**) |
| 중복 기록 | **0** |
| 출처 오염 | 없음 (연결 3개, 시간 구간 겹침 없음) |

**어긋난 6건은 전부 원인이 밝혀져 있다.** 그리고 그중 넷은 내가 일부러 만든 것이다.

| 윈도 | 차이 | 원인 |
|---|---|---|
| 09:37 / 09:38 | −1 / +1 | 대칭이고 총량 보존 → **경계 배정** |
| 12:04 | −13 / −278 | 이미지 배포로 수집기 교체 → **4.009초 단절** |
| 14:52 | −62 / −248 | 소스 단절 실험(의도적) → **2.226초 단절** |

`scripts/verify.sh` 의 1-b 가 연결 사이의 공백을 직접 출력하므로
이 대응이 스크립트 안에서 바로 확인된다.

```
gap_start_kst            gap_end_kst              gap_seconds
2026-09-02 12:04:40.630  2026-09-02 12:04:44.639  4.009
2026-09-02 14:52:01.542  2026-09-02 14:52:03.768  2.226
```

> **99.18% 를 "정확도" 로만 읽으면 안 된다.**
> 이 날은 파티션 재배정, 소스 단절, JobManager kill 을 전부 실행한 날이다.
> 남은 0.79% 는 파이프라인의 오차가 아니라 **내가 일으킨 장애의 크기**이고,
> 그 크기를 초 단위까지 설명할 수 있다는 것이 이 수치보다 중요하다.

### 거래량이 유일하게 민감한 항목이다

소스를 2.226초 끊고 그 윈도를 공식 캔들과 대조한 결과다.

| 항목 | 차이 |
|---|---|
| 시가·고가·저가·종가 | **전부 0** |
| 거래량 | **−1.65% / −1.80%** |

시가·종가는 양 끝점이고 고가·저가는 극값이라, 중간 체결을 36건쯤 잃어도
값이 바뀌지 않는다. 거래량만 합이라서 잃은 만큼 정확히 줄어든다.

**즉 OHLC 일치율만으로는 유실을 탐지할 수 없다.**
이 문서가 거래량 일치율을 대표 수치로 쓰는 이유이고,
`scripts/verify.sh` 가 `vol_ok` 를 채점의 중심에 두는 이유다.
자세한 것은 `failure-notes.md` 의 "소스가 2.2초 끊겼을 때 무엇을 잃는가".

### 두 싱크는 값이 같고 기록 횟수만 다르다 (2026-09-04)

같은 캔들 스트림을 ClickHouse 와 Iceberg 에 동시에 흘린 뒤
TaskManager 를 강제 종료해 "쓴 뒤 커밋 전" 구간을 만들었다.

| | 윈도 | 기록된 행 | 중복된 윈도 |
|---|---|---|---|
| ClickHouse (at-least-once) | 20 | **22** | **2** |
| Iceberg (exactly-once) | 20 | **20** | **0** |

중복은 정확히 **09:40 윈도**, 즉 09:41:25 에 죽였을 때 처리 중이던 그 분이다.
두 행의 값이 같다(`[7161, 7161]`) — 부분 재계산이 아니라 온전한 중복이다.

**값 자체는 완전히 일치한다.** 중복을 제거하고 대조하면
시가·고가·저가·종가·거래량·체결 수가 전 항목 같다.

```sql
-- ClickHouse 한 엔진에서 두 저장소를 대조할 수 있다
WITH ice AS (SELECT * FROM iceberg('http://minio:9000/warehouse/rtp/candles_dual/', ...))
SELECT countIf(i.volume = c.volume) ... FROM ice i INNER JOIN ch c USING (symbol, window_start)
```

> 이 표가 두 번째 싱크를 붙인 이유다.
> "at-least-once 와 exactly-once 는 다르다" 는 문장 대신
> **같은 입력·같은 장애에서 숫자가 갈리는 것**을 남기고 싶었다.

## 여기까지 오는 데 걸린 함정

### 공식 캔들의 `timestamp` 는 윈도 **종료** 시각이다

첫 대조에서 거래량이 평균 59% 어긋났다. 그런데 차이가 **양방향**이었다 —
내 쪽이 적은 게 44건, 많은 게 40건.

프레임 유실이라면 한쪽으로만 어긋나야 한다. 소스가 lossy 하다는 것을 알고 있었으므로
"유실 때문" 이라고 결론 내리기 쉬운 상황이었는데, **부호가 갈린다는 사실 하나가 그 해석을 막았다.**
양방향 오차는 값이 새는 게 아니라 **정렬이 어긋난** 모양이다.

1분씩 밀어서 대조해 보니 결과가 명확했다.

| 시프트 | 거래량 일치 | 종가 일치 |
|---|---|---|
| 0 | 0 / 84 | 39 / 84 |
| **+60초** | **84 / 84** | **84 / 84** |
| −60초 | 0 / 84 | 23 / 84 |

즉 공식 캔들이 `08:42` 로 표시한 봉은 `08:41:00 ~ 08:42:00` 구간이다.
**API 문서에 이 사실이 없다.** 적재 시점에 시작 시각으로 정규화하도록 고쳤다 —
컬럼 이름이 `window_start` 인데 종료 시각이 들어가 있으면
이 테이블을 쓰는 모든 쿼리가 조용히 1분씩 어긋난다.

> 이 항목이 이 프로젝트에서 가장 값진 결과다. 집계 로직은 처음부터 맞았고,
> 틀린 것은 **정답지를 읽는 방법**이었다. 그리고 그것을 구분해 준 것은
> 오차의 크기가 아니라 **오차의 부호 분포**였다.

## 아직 측정하지 않은 것

- **워터마크 설정별 정확도 변화** — 아래 표가 이 프로젝트에서 가장 설명력 있는 결과물이 된다.
  현재 조건(지연 5초)에서 이미 100% 일치라 **차이를 만들려면 더 가혹한 조건이 필요하다.**
  워터마크를 0에 가깝게 줄이거나, 리플레이로 지연 도착을 주입해야 한다.

| 워터마크 지연 | 파티션 | 소비 상태 | 늦어서 버린 체결 |
|---|---|---|---|
| 0초 | 1 | 실시간 추종 | 4,255 |
| 0초 | 3 | 실시간 추종 | 460 |
| 0초 | 3 | 캐치업 | 0 |
| 60초 | 3 | 캐치업 | 0 |
| 300초 | 3 | 캐치업 | 0 |

동일 입력(리플레이 8,000건, 지연 10% 주입, 최대 180초, seed 7) 기준.

**워터마크 지연 열만 보면 아무 설명력이 없다.** 설명력을 갖는 것은 파티션 수와
소비 상태다. 아래에 원인을 적었다.

### 워터마크 지연만으로는 차이가 나지 않았다 — 원인 규명

리플레이로 지연 도착을 주입해 워터마크 0 / 60 / 300초를 비교했더니
**세 조건이 완전히 같은 결과를 냈다.** 14윈도, 8,000건, 거래량 315,220, 드롭 0.
설정을 바꿨는데 아무 차이가 없다는 것 자체가 신호였다.

먼저 메커니즘이 고장난 것인지부터 갈랐다. 손으로 체결 3건을 만들어
- `T` (윈도 A)
- `T+300초` (워터마크를 A 너머로 밀어 A를 닫는다)
- `T+30초` (윈도 A, 이제 늦음)

투입했더니 세 번째가 정확히 버려지고 `lateTradesDropped = 1` 이 찍혔다.
**메커니즘은 정상이다.** 그러면 리플레이 레코드들이 실제로는 늦지 않았다는 뜻이다.

입력에 늦은 것이 없어서는 아니었다. 종목별(=파티션별)로 세어 보면 분명히 있다.

```
005930  순서 역전 2,195건, 60초 초과 1,436건, 최대 180초
000660  순서 역전 1,956건, 60초 초과 1,265건, 최대 180초
```

조건을 하나씩 바꿔가며 재보니 답이 나왔다. **같은 입력, 같은 워터마크 설정(0초)** 이다.

| 조건 | 파티션 | 소비 상태 | 늦어서 버린 체결 |
|---|---|---|---|
| 수동 3건 | 1 | 실시간 | 1 / 1 |
| 리플레이 8,000건 | 1 | 실시간 추종 | **4,255** |
| 리플레이 8,000건 | 3 | 실시간 추종 | **460** |
| 리플레이 8,000건 | 3 | 캐치업 | **0** |

#### 결론: 워터마크 지연은 실제 허용치를 결정하지 않는다

워터마크는 200ms 주기로 "지금까지 본 최대 이벤트타임 − 지연" 으로 생성된다.
어떤 레코드가 처리되는 순간 워터마크가 얼마나 전진해 있는지는 설정이 아니라
**그 시점의 소비 상황**이 정한다.

- **파티션이 많을수록 여유가 커진다.** 워터마크는 파티션별 워터마크의 **최소값**이다.
  파티션마다 소비 속도가 어긋나면 그 최소값이 뒤처지고, 그만큼 늦은 데이터가 살아남는다.
  파티션 1개 → 3개로 늘리자 드롭이 4,255 → 460 으로 줄었다
- **캐치업 중에는 거의 버려지지 않는다.** 밀린 데이터를 몰아 읽을 때는
  워터마크가 처리 위치를 따라가지 못한다. 200ms 한 틱 사이에 수천 건이 지나간다

**같은 잡이 밀렸다가 따라잡는 동안과 실시간을 유지할 때 서로 다른 결과를 낸다.**
이것이 이 실험에서 나온 가장 중요한 사실이다. 워터마크 지연은
"최소한 이만큼은 기다린다" 는 하한이지, 실제 허용치가 아니다.

> `validation.md` 첫 실측에서 라이브 파이프라인이 공식 캔들과 99% 일치한 것도
> 이 여유 덕을 봤을 수 있다. 파티션 3개에 소비가 실시간을 유지하는 조건이었다.
> **그 정확도는 설정이 보장한 것이 아니다.** 부하가 올라가 잡이 밀리기 시작하면
> 같은 설정으로도 결과가 달라질 수 있다.

#### 이 실험을 재현하려면

유한한 리플레이는 마지막 윈도들이 닫히지 않아 그대로는 비교가 안 된다.
리플레이가 끝난 뒤 **미래 시각의 센티널 레코드**를 종목마다 하나씩 넣어
워터마크를 밀어야 모든 윈도가 닫힌다. 위 결과도 그렇게 얻었다.

- **장 마감 구간** — 마감 동시호가(15:20~15:30)는 아직 대조하지 않았다.
  개장 직후(09:00)는 확인했다 — 체결 1,601건 / 거래량 732,619 로 하루 중 가장 몰리는
  분인데 정확히 일치했다
- **장기 구동** — 아직 두어 시간 구간이다. 체크포인트가 누적되고 상태가 커진 뒤에도
  같은 정확도가 유지되는지는 별개 문제다
- **09:05·09:06 경계 배정 차이의 원인** — 위 참고

## 소스 유실률 (2026-08-31 실측)

`recv_seq` 는 연결별로 1씩 증가하므로 공백이 곧 유실이다.

| 연결 | 수신 | seq 범위 | 공백 | 유실률 |
|---|---|---|---|---|
| `f4ebd52f49ac` (실제 수집기) | 117,730 | 1 ~ 117,730 | **0** | **0%** |

문서가 lossy 하다고 명시한 스트림인데 **이 구간에서는 한 건도 잃지 않았다.**
장 초반 두어 시간, 초당 60건 수준에서의 결과다. 부하가 더 높은 구간이나
장시간 구동에서도 그런지는 별개 문제다.

Kafka → Flink 구간도 확인했다 — 142개 윈도에서 원본 체결 수와 캔들의
`trade_count` 가 전부 일치했다(`rtp.trade_count_check`).

> 이 표가 있어야 "공식 캔들과 다르다" 를 해석할 수 있다.
> 유실률이 0인데 값이 다르면 그건 유실이 아니라 **배정 문제**다 —
> 실제로 09:05·09:06 건이 그렇게 판별됐다.
