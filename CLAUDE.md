# CLAUDE.md

## 이 프로젝트의 성격

거래소 실시간 체결 데이터를 Flink로 집계해 분 단위 캔들(OHLCV)을 만들고,
거래소 공식 캔들과 대조해 정확성을 검증하는 스트리밍 파이프라인.

**개인 사이드 프로젝트이자 포트폴리오다.** public 저장소를 전제로 한다.

```
Exchange WebSocket → Kafka → Flink → ClickHouse → 공식 캔들과 대조 검증
```

## 목적 (중요)

**"돌아가는 파이프라인"이 목적이 아니다.** 스트림 처리에서 결과가 틀어지는 지점을 직접 만나고
해결한 기록을 남기는 것이 목적이다. 정상 동작 코드보다 `docs/failure-notes.md`가 핵심 산출물이다.

작성자는 데이터 엔지니어 4년차로, 실무에서 **CDC 적재**(Debezium→Kafka→Iceberg)와
**Airflow on EKS 운영** 경험이 있다. 이 프로젝트는 실무에서 얇았던 영역 —
**스트림 처리 의미론(이벤트타임 윈도우, 워터마크, exactly-once)과 스트리밍 워크로드의 장애 대응** — 을 채운다.
따라서 이미 아는 영역(K8s 배포 자체, CI/CD, 적재 파이프라인)에 시간을 쓰지 않는다.

## 작업 원칙

- **범위를 넓히지 말 것.** 본업 병행이라 벌리면 미완성으로 끝난다. 종목은 소수만, 대시보드 없음
- **개발 순서: docker-compose로 먼저 동작시킨 뒤 K8s로 옮긴다.** 처음부터 K8s에서 시작하면
  처리 로직 버그와 클러스터 문제가 뒤엉킨다
- **실험하면 기록한다.** 무엇을 일으켰나 → 어떻게 드러났나 → 왜 그랬나 → 어떻게 고쳤나
- **README에 회사 이야기를 쓰지 않는다.** public 저장소다. 개인 커리어 맥락은 별도 워크스페이스에 있다

## 문서 구조

| 파일 | 용도 |
|---|---|
| `docs/design-decisions.md` | 왜 이 구성인지, **무엇을 포기했는지** |
| `docs/failure-notes.md` | ★ 의도적으로 깨뜨리고 고친 기록 — 핵심 산출물 |
| `docs/validation.md` | 공식 캔들 대비 정확도, 설정 변경에 따른 정확도 변화 |

## 스택

Kafka · Apache Flink (Kubernetes Operator) · ClickHouse · Kubernetes

ClickHouse는 학습 목적이 겸해져 있다 — 실무에서 도입을 검토 중인 기술이라 여기서 먼저 다뤄본다.
